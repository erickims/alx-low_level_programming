#include "main.h"

/**
 * _abs - compute the absolute value of an int
 * @n: integer to check
 * Return: absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
